---

# 3.4.2 Ensure /etc/hosts.allow is configured

- name: 3.4.2 - Check if /etc/hosts.allow configuration file exists
  stat:
    path: "/etc/hosts.allow"
  register: hosts.allow_3_4_2
  tags:
    - level-1
    - section-3
    - "3.4.2"
    - scored

- name: 3.4.2 - Ensure /etc/hosts.allow is configured
  copy:
    path: "/etc/hosts.allow"
    content: "ALL: {{ cis_hosts_allow_all_ips }}\n"
  when: hosts.allow_3_4_2.stat.exists is not defined or not hosts.allow_3_4_2.stat.exists
  tags:
    - level-1
    - section-3
    - "3.4.2"
    - scored

- name: 3.4.2 - Ensure /etc/hosts.allow is configured
  lineinfile:
    dest: "/etc/hosts.allow"
    regexp: "^ALL:"
    line: "ALL: {{ cis_hosts_allow_all_ips }}"
  when: hosts.allow_3_4_2.stat.exists is defined and hosts.allow_3_4_2.stat.exists
  tags:
    - level-1
    - section-3
    - "3.4.2"
    - scored
