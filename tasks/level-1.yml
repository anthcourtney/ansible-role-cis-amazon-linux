# Standards: 0.11
---

- include_tasks: "{{ item }}"
  when: "'{{ (item | basename | splitext)[0] }}' not in cis_level_1_exclusions"
  with_items: "{{ lookup('fileglob', 'level-1/*.yml').split(',') | sort }}"
